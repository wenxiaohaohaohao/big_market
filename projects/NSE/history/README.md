# History

Git is the normal version history. This directory is only for named milestone
snapshots that include provenance: date, purpose, source commit, and whether
the snapshot is superseded.

Do not create parallel files such as `main_final_v2.tex` here.

## Current named snapshot

`legacy_2026-07-24_pre_axis_reset/` preserves the model contracts and plans
that preceded the confirmed access--capture axis. Its files are `SUPERSEDED`
and may not be treated as active project instructions.
